import { StrongPasswordDirective } from './strong-password.directive';

describe('StrongPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new StrongPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
